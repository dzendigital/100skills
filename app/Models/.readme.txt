# основные models это:
# menu, page
# permission, role, user

# если необходимо уточнить конфигурацию моделей (в т.ч поля, название таблиц и пр.), то нужно
# скопировать из корня в директорию проекта /app/Models/{Project_name}/
# изменить привязки в migrations 
